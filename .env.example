# Email Configuration
CORREAGRO_EMAIL=your_email@example.com
# Note: CORREAGRO_PASSWORD is only used for basic auth (non-Office365 servers)
# For Office 365 (Pulgarin), OAuth 2.0 is used instead (no password needed)
CORREAGRO_PASSWORD=your_password_here

# OAuth 2.0 Configuration (Azure AD App Registration)
# Required for Office 365 IMAP authentication (Pulgarin client)
# See AZURE_APP_REGISTRATION.md for step-by-step registration instructions
AZURE_CLIENT_ID=your-azure-client-id-here
# AZURE_TENANT_ID: Use "common" for multi-tenant or your specific tenant ID
AZURE_TENANT_ID=common

# Somex SFTP Configuration
SFTP_SOMEX_PASS=your_sftp_password_here

# Application Settings (optional overrides)
# LOG_LEVEL=INFO
# OUTPUT_DIR=output
